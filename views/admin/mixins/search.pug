mixin search(keyword)
    form(id="form-search")
          .input-group
            input(
              type="text"
              name="keyword"
              class="form-control"
              placeholder="Nhập từ khóa..."
              value=keyword
            )
            button(
              class="btn btn-success"
              type="submit"
            ) Tìm kiếm

